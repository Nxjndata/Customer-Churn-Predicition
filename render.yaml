services:
  - type: web
    name: Churn Predictor
    env: python
    buildCommand: |
      pip install --upgrade pip setuptools wheel
      pip install -r requirements.txt
    startCommand: python app.py  # or whatever your entry point is
