services:
  - type: web
    name: churn-predictor
    env: python
    region: singapore
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    rootDir: .
    plan: free
